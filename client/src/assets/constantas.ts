const url: string = 'http://localhost:5000';
const authUrl: string = '/auth';
const profileDataUrl: string = '/put-profile-data';

const lineTestToken = 'line_test_token';

const TEST_TIME_MAX = 300;

export default {
  url, authUrl, profileDataUrl, lineTestToken, TEST_TIME_MAX
} 