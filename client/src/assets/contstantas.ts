const url: string = `http://localhost:5000`;
const auth: string = `/auth`;

const lineTestToken = 'line_test_token';

const TEST_TIME_MAX = 300;

export default {
  url, auth, lineTestToken, TEST_TIME_MAX
} 